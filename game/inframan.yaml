version: "v1"

service:
  port: 8888
  scaling:
    type: concurrency
    min_replicas: 0
    max_replicas: 2
    target: 100
  resources:
    cpu_request: "100m"
    memory_request: "256Mi"

build:
  method: docker
  dockerfile: ./Dockerfile

env:
  APP_NAME: "game-from-yaml"
